{
  "name": "@hitpay-samples/collect-saved-method",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "HitPay MPP COLLECT — saved-method binding. First hit returns 402 with a setup URL; once the buyer approves, subsequent hits charge silently.",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hit-pay/mpp": "0.0.1-beta.1",
    "@hono/node-server": "^2.0.0",
    "dotenv": "^17.4.2",
    "hono": "^4.12.15"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0"
  }
}
